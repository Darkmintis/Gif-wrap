export type FrameData = {
  id: string;
  url: string;
  delay: number;
  width: number;
  height: number;
};

export type OutputConfig = {
  width: number;
  height: number;
  useCustomDimensions: boolean;
};
